# 링크모음 마스터 — 점검·변경 이력

`master.html`의 도구 카드가 **실제 배포된 도구와 맞는지** 확인한 기록입니다.
새 도구를 추가하거나 설명을 고칠 때, 여기에 한 줄 남겨 두면 다음에 헷갈리지 않습니다.

> ⚠️ 이 저장소는 public 입니다. **교사용 암호·주간 학생 코드·시크릿은 절대 적지 마세요.**

---

## 2026-09-17 · [안전] 새던 곳 세 군데를 막고, 스스로 점검하게 했습니다

바이브 코딩 연수 자료(2026-09-09)의 안전 원칙에 비춰 전체를 훑어본 결과입니다.

**① README 에 적혀 있던 교사용 암호를 지웠습니다**

`README.txt` 에 `python build_lock.py --pw <암호>` 라고 암호가 그대로 적혀 있었습니다.
이 저장소는 public 입니다. 다행히 그 암호는 이미 바뀐 뒤라 그것으로는 열리지 않습니다
(브라우저에서 직접 넣어 확인했고 "암호가 맞지 않습니다" 가 떴습니다).

문제는 따로 있었습니다. **안내대로 따라 하면 공개된 옛 암호로 되돌아갑니다.**
게다가 `--pw` 는 원래 필요 없었습니다 — `build_lock.py` 는 `_weekly/secret.json` 의
`teacher_pw` 를 알아서 씁니다. 안내가 틀린 데다 위험하기까지 했던 셈입니다.

- `README.txt` — 암호를 지우고, 기본은 `python build_lock.py` 라고 고쳤습니다.
  `--pw` 는 "바꿀 때만" 쓰는 것으로 적었습니다. 암호 바꾸는 절차도 새로 넣었습니다.
- `build_lock.py` — 빌드 끝에 암호를 통째로 찍던 것을 `시험***** (9자)` 처럼 가렸습니다.
  연수 시연이나 화면 공유 중에 그대로 보이기 때문입니다. `--show-pw` 로 전체를 볼 수 있습니다.
- ⚠️ 옛 암호는 커밋 `4e64f24` 에 남아 있습니다. 지금 암호와 다르니 당장 위험하진 않지만,
  **암호를 새로 바꾸실 때 그 값과 겹치지 않게** 하세요.

**② 외부 CDN 두 개를 저장소 안으로 들여왔습니다**

`master.html` 이 `cdn.jsdelivr.net` 에서 lz-string 과 qrcode-generator 를 받아 썼습니다.

- 학교 망이 그 주소를 막으면 [링크+QR 만들기]가 멈춥니다. 실제로 재현했습니다.
- 더 걸렸던 것은, `master.html` 이 **교사용 암호를 넣는 화면**이라는 점입니다.
  남의 서버에서 받아 온 코드가 그 화면에서 같이 돕니다.

`vendor/` 에 직접 담고 거기서만 부릅니다(`vendor/README.txt` 에 출처·라이선스·SHA-384).
확인 결과 **바깥으로 나가는 요청이 하나도 없습니다.** 인터넷이 끊겨도 QR이 만들어집니다.

**③ 분야·과목 목록을 tools.enc 안으로 옮겼습니다**

도구 이름은 암호화돼 있었지만, 분야와 과목 이름은 `master.html` 안에 평문으로 있었습니다.
`hubsrc` 는 `type="text/plain"` 이라 **잠금 화면이 떠 있어도 소스 보기만 하면 다 보였습니다.**
「🔒 내부 · 관리용 (공유하지 마세요)」 라는 분류명까지요.

- 목록은 `areas.js`(`.gitignore`, 내 PC에만)로 옮기고 `tools.enc` 안에 암호화해 넣습니다.
- `python build_lock.py` 를 한 번 돌리면 **자동으로 옮겨집니다.** 따로 하실 것은 없습니다.
- 그전까지는 `master.html` 안의 목록을 그대로 씁니다 — 지금 올라간 `tools.enc` 로도
  똑같이 열립니다(옛 형식과 새 형식을 `lock.js` 가 둘 다 받습니다. 실제로 확인했습니다).
- 분야 이름으로 내부용을 찾던 `INTERNAL` 상수를 없앴습니다. 그 이름이 파일에 남기 때문입니다.
  이제 **목록의 맨 끝 분야**를 내부용 자리로 약속합니다 — 새 분야는 그 앞에 넣으세요.

**④ 두 파일이 어긋나는 것을 기계가 보게 했습니다**

`master.html` 과 `links/s.html` 의 잠금 코드(`gB64`·`gateTry`·`gateUnseal`·`gateMount`)는
같은 내용이어야 합니다. 지금까지 주석에 "고치면 둘 다 고칠 것" 이라고 적어 두고 손으로
지켜 왔고, 지금도 정확히 같습니다(91줄). 다만 바쁠 때 한 번 놓치면 알아채기 어렵습니다.

- `check_sync.py` — 두 파일을 견줘 보고 다르면 어느 줄이 다른지 보여 줍니다.
  `build_lock.py` 가 빌드할 때 자동으로 같이 돌립니다.
- 저장소가 둘로 나뉘어 있어 파일 하나로 합칠 수는 없습니다.
  `s.html` 을 `links-master` 로 옮기면 학생이 주소를 잘라 전체 목록을 보게 되니까요.
  그래서 "합치기" 대신 "어긋나면 알려 주기" 로 갔습니다.

**그리고 — 손으로 눌러 보던 것을 스크립트로**

`check_app.js` 를 넣었습니다. 진짜 브라우저로 열어 연수 자료가 말하는 세 가지 상황을 봅니다.

| 상황 | 보는 것 |
|---|---|
| 정상 | 암호로 열림 · 카드가 그려짐 · 링크와 QR · 내보내기 파일 |
| 잘못된 입력 | 틀린 암호로는 안 열림 · 목록도 안 나옴 |
| 다시 사용 | 새로고침해도 기억된 암호로 바로 열림 |

그 밖에 콘솔 오류 없음, 바깥으로 나가는 요청 없음, 모바일 가로 스크롤 없음,
잠긴 소스에 과목 이름이 새지 않음까지 함께 봅니다.
`node check_app.js <교사용 암호>` 로 돌립니다.

실제 검증: 시험용 목록과 시험용 암호로 빌드부터 화면까지 전 과정을 돌렸습니다.
분야 목록을 옮기기 전과 옮긴 뒤, 그리고 **지금 올라가 있는 옛 형식 `tools.enc` 로도**
세 경우 모두 전부 통과했습니다.

---

## 2026-09-17 · [잠금] 코드는 교과서·기출이 든 도구에만 묻습니다

**증상** — 2026-09-11 잠금을 넣은 뒤로 공유 링크·내보내기 파일이 **무엇을 담았든** 코드를 물었습니다.
자작 문항으로 만든 도구도 마찬가지라, 「NCS ① 개념·게임」처럼 코드가 필요 없는 것까지
학생이 코드부터 넣어야 했습니다.

**고친 것** — 고른 도구를 `cr`(외부 기출·저작물 포함 · 카드의 ⚠️) 하나로 둘로 나눕니다.

| 고른 내용 | 나가는 것 | 받는 사람 |
|---|---|---|
| 전부 자작·공개 | `s.html?d=…` | 코드 없이 바로 열림 |
| 섞임 | `s.html?d=…&e=…` | 자작은 바로 · 목록 위 작은 줄에서 코드를 넣으면 ⚠️ 것들이 더해짐 |
| 전부 저작물 | `s.html?e=…` | 예전대로 통째 잠김 (목록 자체를 감춤) |

내보내기 HTML 도 같습니다. 전부 자작이면 파일 안에 잠금 코드가 아예 안 들어갑니다.

- `links/s.html` · `master.html` 의 `gateMount` 에 `compact` 모드를 넣었습니다 —
  화면을 막지 않고 목록 위 한 줄로만 뜹니다. 두 파일의 이 함수는 **같은 내용을 유지**하세요.
- `tools.js` — 「NCS ① 개념·게임」의 `cr:true` 를 **뗐습니다**. 문항이 전부 자작이고
  저장소에 교재 그림이 하나도 없습니다(`dist:"수업"` → `"공개"`).
  ※ 고친 뒤 `python build_lock.py` 로 `tools.enc` 를 다시 구워야 반영됩니다.
- 예전 `?d=` 링크는 그대로 열립니다. `?e=` 만 있는 링크도 그대로입니다.

실제 클릭 검증: 시험용 키 묶음을 따로 만들어 세 경우를 모두 브라우저에서 눌러 확인 —
섞임(자작 2개 바로 보이고, 코드 넣으니 2개 더해짐) · 전부 자작(잠금 줄 없음) ·
전부 저작물(전체 화면 잠금 → 코드로 열림) · 내보내기 HTML 도 같은 세 경우.

---

## 2026-09-14 · [잠금] 「🔑 기억된 암호 지우기」 단추

허브 맨 아래 버전 줄에 **🔑 기억된 암호 지우기** 단추를 달았습니다.
한 번 넣은 암호는 기기(브라우저)가 기억해 다음부터 바로 열리는데, 공용 PC에서 쓴 뒤나
교사용 암호를 바꾼 뒤에 그 기억을 지울 방법이 화면에 없었습니다.

- 누르면 먼저 확인창이 뜹니다. **[확인]** 을 누르면 지우고 새로고침되어 잠금 화면이 다시 나옵니다.
- 지우는 것: 허브에서 넣은 것(`hub_pw_v1`) · 도구 공용(`hong_pw_v1`) · 잠긴 공유 링크에서 넣은 것(`share_pw_v1`).
- 도구들이 같은 주소라 **같은 브라우저의 다른 잠긴 도구도 다시 묻습니다**(확인창에 적어 둠).
- 교사용 암호 자체가 바뀌는 것은 아닙니다.

실제 클릭 검증: 암호로 열기 → 새로고침 시 자동 열림 → 지우기 [취소] 면 그대로 → [확인] 이면 세 값 모두 지워지고 잠금 화면.

---

## 2026-09-11 · [잠금] 공유 링크·내보내기 파일도 코드를 넣어야 열리게

**점검해 보니** master 본체는 기기마다 암호를 따로 기억하므로 다른 기기에서는 원래 암호를 물었습니다.
암호 없이 열리던 것은 **master가 만들어 내보내는 것들**이었습니다 —
[🔗 링크 + QR] (`links/s.html?d=…`) 과 [📄 HTML 내보내기] 파일은 도구 목록이
압축만 돼 있고 잠금이 전혀 없었습니다. 그래서 받은 사람이 그대로 열었습니다.

이제 **새로 만드는 [링크 + QR] 과 [HTML 내보내기]는 목록을 암호화**합니다.

- 받은 사람은 **이번 주 코드(8자리)나 교사용 암호**를 넣어야 목록이 풀립니다.
- 보낸 사람 기기(허브·도구에서 이미 암호를 넣은 기기)는 기억된 암호로 바로 열립니다.
- 받은 사람이 한 번 넣으면 그 기기는 기억합니다. 주간 코드는 그 주가 지나면 다시 묻습니다.
- 링크 주소는 `?d=` 대신 `?e=` 로 나갑니다. 도구 2개 기준 389자, QR v13 (전과 거의 같음).
- 암호화 키(공유 키)는 `tools.enc` 의 감싼 키 안에만 있고, 시크릿에서 뽑으므로
  **다시 빌드해도 바뀌지 않습니다** — 나눠 준 링크가 빌드 때문에 깨지지 않습니다.
- 교사용 암호와 주간 코드는 **바뀌지 않았습니다** (빌드 전후 같은 암호·코드로 열리는 것 확인).

**그대로 둔 것 (일부러)**
- 이미 나눠 준 예전 `?d=` 링크·QR — 수업 중인 QR 을 깨지 않도록 지금처럼 바로 열립니다.
- 다른 선생님께 드리는 admin 링크 → 학생용 user 링크 — 그 반 학생들은 이 주간 코드를 모르므로 잠그지 않았습니다.
- 개별 도구 [📋 복사]·[📱 QR] — 도구 주소 그대로라, 잠금은 도구마다 자기 잠금(`*.enc`)을 따릅니다.

**함께 고친 버그 — [HTML 내보내기] 파일이 9/8부터 빈 화면이었음**
9/8 에 설명의 `<b>` 를 살리는 `escDesc` 를 내보내기 파일에도 넣었는데, 템플릿 문자열 안에서
정규식의 `\/` 가 `/` 로 바뀌어 **파일의 스크립트 전체가 문법 오류로 멈췄습니다**(카드가 하나도 안 나옴).
`\\/` 로 고쳤고, 내보낸 파일의 스크립트가 문법 오류 없이 도는지 확인했습니다.
9/8~9/11 사이에 내보낸 파일을 받은 분이 있으면 새로 내보내 주세요.

고친 파일: `build_lock.py`(공유 키 넣기) · `lock.js`(`shareKit()`) · `master.html`(암호화·잠금 코드) ·
`links/s.html`(`?e=` 잠금 화면). 배포 순서는 **`links/s.html` 먼저, 그다음 이 저장소** —
반대로 하면 잠깐 동안 새 링크가 옛 뷰어에서 "불러오지 못했어요" 로 뜹니다.

---

## 2026-08-31 · [점검] 다시 빌드할 때 카드가 조용히 사라지는 것을 막음

`tools.js` 는 `.gitignore` 라 내 PC에만 있습니다. 다른 기기·세션에서 추가한 카드는
`tools.enc` 에만 들어 있어서, **낡은 `tools.js` 로 그냥 다시 빌드하면 그 카드가 사라집니다.**
(2026-08-25 에 실제로 2장이 사라질 뻔했습니다.)

이제 `build_lock.py` 가 빌드 전에 **지금 올라가 있는 `tools.enc` 를 같은 교사용 암호로 풀어
도구 이름을 맞춰 봅니다.**

- 빠지는 카드가 있으면 이름을 늘어놓고 **빌드를 멈춥니다.**
- 정말로 빼려는 것이면 `--allow-remove` 를 붙입니다.
- 새로 들어가는 카드는 `+ 이름` 으로 보여 줍니다.
- 암호가 다르거나 파일 형식이 바뀌어 못 풀면, 경고만 하고 예전처럼 그냥 진행합니다.

```
python build_lock.py --pw <교사용 암호>
```

점검 당시 확인: 배포본 76장 · 로컬 `tools.js` 76장, **이름과 순서까지 일치** — 지금은 위험 없음.


## 2026-08-31 · 기초학력 과제 링크 만들기 카드 추가 (75 → 76장)

- 📋 **기초학력 과제 링크 만들기 (교사용)** 를 `인문 · 기초학습` 에 추가했습니다.
  선생님이 과목(국·영·수)과 학년(중1~고3)을 골라 **과제 링크**를 만들어 뿌리는 도구
  (`gicho-hakryeok-master/과제만들기.html`). 도구 자체는 2026-08 에 만들어 배포돼 있었는데
  허브에 카드가 없어 찾아 쓸 수 없었습니다.
- 같은 분야의 카드 3장에 `ord` 를 넣어 **진단 → 과제 만들기 → 사회생활** 순서를 고정했습니다.
- 카드에 `repo` 와 `url` 을 함께 넣었습니다. `url` 만 있으면 `?rc=` 가 붙지 않아
  선생님이 시트 주소를 손으로 다시 넣어야 합니다. 짝으로 `과제만들기.html` 도
  **들어온 `?rc=` 를 결과수집 칸에 자동으로 채우도록** 고쳤습니다(gicho 저장소 커밋).

**함정** — 카드 `desc` 의 `<b>` 는 허브 목록에서 **그대로 글자로 보입니다**(`esc()` 처리).
공유용 내보내기 쪽도 `esc(t.desc)` 라 마찬가지입니다. 새 카드에서는 태그를 뺐습니다.
(기존 카드 2장에 `<b>` 가 남아 있습니다 — `그림 문제 만들기`, `제도 도해 자료실`.)

---

## 2026-08-28 · 전체 링크 점검 + 빠져 있던 도구 4개 연결 (72 → 75장)

**링크 점검** — 카드 72장의 주소를 전부 열어 확인했습니다. **끊긴 주소 없음**(70장 200 OK).
나머지 2장(`phone-pc-remote`, `timetable-substitute`)은 **private 저장소**라 로그인해야 열립니다 — 정상.

**허브에 없던 배포 도구 4개를 추가**했습니다. GitHub 저장소 목록과 카드 목록을 대조해서 찾았습니다.

- 🏭 **공업 일반 (2022 개정)** — `gongeop-ilban-master` · 새 분야 밴드 `🏭 공업 일반 (공통 교과)` 를 AREAS 에 등록
- 🧊 **공조냉동 자동제어** — `gongjo-master` · `공조냉동 · 에너지제어` (배포는 2026-07 에 됐는데 카드가 없었음)
- 📢 **지금 지원할 수 있는 공고 (고졸채용)** — `gojol-chaeyong` · `취업 · NCS 직업기초능력`
- 📋 **연구수업 지도안 (교수·학습 과정안)** — `jidoan-master` · `교사 도우미 (수업 · 행정)`

**중복 카드 1장 정리** — `드론 전체 모아보기` 와 `드론 이론 1~6차시 (묶음)` 이
**같은 주소**(`drone-master/`)를 가리키고 있어 한 장으로 합쳤습니다(이론 설명은 살려서 옮김).

**함정** — 카드 이름이 `master.html` 안에 그대로 있으면 빌드가 막힙니다(평문 유출 자체 검사).
분야/과목 이름은 `master.html` 의 AREAS 에 남으므로, **카드 이름을 과목 이름과 똑같이 지으면 안 됩니다.**
그래서 카드 이름은 `공업 일반` 이 아니라 `공업 일반 (2022 개정)` 으로 했습니다.

---

## 2026-08-24 · 카드 2장 추가 (제출 읽기·받기) + 뒤로 버튼 전면 적용

- **📥 제출파일 읽기도구 (교사용)** · **📤 제출 받는 법 (드라이브)** 를
  `인공지능 활용 (AI와 직업생활)` 에 추가 (83 → **85장**).
  둘 다 교사용이라 `repo` 없이 `url` 로 넣어 `?rc=` 가 붙지 않게 했습니다.
- **뒤로 / 목록으로 버튼**을 41개 저장소 110개 페이지에 공용 위젯으로 붙였습니다
  (`links/backbar.js` 한 줄). 적용 전에는 102장 중 **75장에 돌아갈 방법이 없었고**,
  그중 34장은 제출까지 되는 페이지였습니다.
  `reset.js`(🧹 기록 초기화)와 자리가 겹쳐서 그 위로 쌓이게 처리했습니다.

**tools.enc 를 고친 방법 (다음에 또 할 때)** — 내용키(`ck`)와 감싼 키 목록(`keys`)은
그대로 두고 `data` 만 다시 봉하면, 교사용 암호도 이미 나가 있는 주간 학생 코드도
전부 그대로 통합니다. `_weekly/secret.json` 없이도 카드만 고칠 수 있습니다.
(고친 뒤 교사용 암호 + 이번 주 학생 코드 양쪽으로 열어 85장이 나오는지 확인했습니다.)

---

## 확인 방법 (다음에 또 할 때)

카드 설명을 **눈으로 믿지 말고** 배포본을 열어 숫자를 직접 셉니다.

| 확인할 것 | 방법 |
|---|---|
| 문항 수 | 저장소의 `data*.js`를 받아 문항 객체 개수를 셈 |
| **런타임 생성 문항** | 반복문으로 만드는 문제는 정적으로 세면 틀림 → **node로 실행해서** `QUESTIONS.length` 확인 |
| 슬라이드 장수 | `board.html`/`lesson.js`의 슬라이드 배열 길이 |
| 수업모드 지원 | 그 도구 HTML이 `class-mode.js`를 부르는지 |
| 링크 유효성 | `repo:`/`url:`이 가리키는 파일이 실제 저장소에 있는지 |

---

## 2026-08-21 · 전체 재점검

- **공기조화설비 수업 슬라이드 35장 → 41장** (SLIDES 배열 실측)
- 3D프린터 문항 수는 **521이 맞음**을 확인.
  계산형 문항이 실행 시 반복문으로 생성돼, 정적으로 세면 378로 **과소집계**됨.
  → 이런 도구는 반드시 node로 실행해서 셀 것.
- 구조 점검: 카드 이름 중복 0 · 분류 누락 0 · 링크 전부 유효 · 인라인 JS 문법 정상 · 콘솔 오류 0

**알아둘 한계** — 공유 링크 QR은 도구 **39개까지** 생성됩니다.
그 이상 고르면 주소가 길어져 QR이 안 만들어지고 안내 문구가 뜹니다(링크 자체는 정상).

## 2026-08-21 · 설명 전수 대조 (카드 64장)

배포본을 직접 열어 대조하고, 사실과 다른 것만 고쳤습니다.

| 도구 | 고친 내용 |
|---|---|
| 시퀀스·PLC | 설명 12자 → 실제 '자동화 공장 가동' **8스테이지 60단계** 게임 내용 |
| 사회생활 학습도구 | 설명 16자 → 실제 **11단원 27개 도구** 목록 |
| 자동화설비 필기 CBT | 이론 배우기 6단원 + 수업모드·전자칠판 누락분 추가 |
| 전기기능사 필기 CBT | 반복기출 500제·핵심이론 25쪽·출제기준 20항목 추가 |
| NCS 직업기초능력 | 실전 모의고사 10회·추천 세트 추가 |
| 열처리 마스터 | 게임 4종 → **5종**(실측), 영상 강의 4편 추가 |
| 제도 마스터 허브 | 도구 14개 → **15개** |
| 호주 현장학습 기록장 | '구글폼 제출'(현재 없음) → 보고서 PDF·사진만 제출 |
| 기초학력 진단 | '중학생' → **중1~고3** (도구 자체 소개 기준) |
| 생기부 문장 생성기 | 창의적 체험활동·글자수 제한·Gemini 키 방식 반영 |
| 컴활 1급 CBT / 2급 개념게임 | 모의고사 종류·핵심요약 누락분 추가 |
| 컴활 실기 3종 | 실측 문제 수(46·49·67) 보강 |
| 도면읽기 마스터 | 부서 3자격증 → 종합시험 개방 구조 반영 |

## 2026-08-21 · 수업모드 표시 추가

- 각 저장소가 `class-mode.js`를 부르는지 확인해 **`cm:true`** 부여 → 🎓수업모드 배지
- **[🎓수업모드 되는 것만]** 버튼으로 수업용 묶음을 한 번에 골라 공유

## 2026-08-21 · 분류·디자인 개편

- 분류를 **분야 → 과목 2단계**로 재편 (전공은 기계·설비 / 항공·드론 / 금속·재료 / 전기 / 철도 / 컴퓨터)
- 줄글 목록 → **카드 그리드** (설명·타입 태그·배지)
- 접기 헤더 글자 크기를 **대(15.5px) > 중(13.5px) > 소(13px)** 순으로 정리
- 컴활 2급 중복 등록 3건 제거

---

## 주간 학생 코드에 대해 (자주 헷갈리는 점)

**다시 빌드해서 배포해도 학생 코드는 바뀌지 않습니다.**

코드는 `HMAC(시크릿, 접두어 + 그 주 월요일)`로 계산됩니다.
시크릿·접두어·기준일은 `_weekly/secret.json` 한 곳에 있고 **빌드할 때 새로 만들지 않습니다.**
그래서 몇 번을 다시 배포해도 같은 주에는 같은 코드가 나옵니다.

빌드마다 바뀌는 것은 코드와 **무관한 값**들입니다 — 내용키·salt·`lock.js?v=` 캐시 표식.

> 🔒 **`_weekly/secret.json`이 사라지면 그동안 나눠 준 코드가 전부 무효가 됩니다.**
> git에 올라가지 않으므로(의도된 것), **따로 백업해 두세요.**

## 2026-08-25 · 신재생에너지 마스터 추가 (85 → 86장)

- **신재생에너지발전설비기능사(태양광)** 분야 신설, 필기 문제풀이 카드 1장 추가
  (`sinjaesaeng-master` · 적중예상문제 325문항 · 문항과 교재 그림 모두 암호 잠금)
- **로컬 `tools.js` 가 배포본보다 2장 뒤처져 있던 것을 되살렸습니다.**
  `tools.js` 는 `.gitignore` 라 다른 세션이 추가한 카드가 내 PC에는 없습니다.
  그대로 다시 빌드하면 그 카드들이 허브에서 **조용히 사라집니다.**

> **다음에 카드를 추가할 때 반드시 먼저 할 것** — 내 `tools.js` 와 배포본 `tools.enc` 를 대조하세요.
> `tools.enc` 를 교사용 암호로 풀어 이름 집합을 비교하면 빠집니다.
> (이번에 `제출파일 읽기도구 (교사용)`, `제출 받는 법 (드라이브)` 두 장이 이 방식으로 되살아났습니다)

## 2026-08-25 · 신재생에너지 마스터에 개념 파트 추가 (카드 수 변동 없음)

- 개념 10단원 75섹션이 붙어 카드 설명을 갱신 (문제풀이 → 개념 + 문제풀이)
- 대조 결과 로컬 `tools.js` 86장 = 배포본 86장, 빠진 카드 없음

## 2026-09-03 · 「전자 기계 이론」 분야 신설, 카드 1장 추가

- **전자 기계 이론** 분야를 새로 만들고 카드 1장 추가
  (`jeonja-gigye-master` · 교과서 Ⅰ~Ⅳ단원 · 배우기 13주제 + 신호 사슬 + 짝 맞추기 + 종합시험 20문항)
- 대조 결과 배포본 76장 → 77장, 사라진 카드 없음 (빌드가 자동으로 확인)
- Ⅴ 제어 장치는 그 도구에 없고 기존 `sequence-plc-master` 로 이어집니다
